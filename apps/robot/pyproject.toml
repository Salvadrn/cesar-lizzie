[project]
name = "adaptai-robot"
version = "0.1.0"
description = "Adapt AI Medical Follower Robot - Raspberry Pi Controller"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
