{
  "name": "jarvis-worker",
  "version": "1.0.0",
  "description": "JARVIS Backend - Cloudflare Worker con Claude API",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
