import SwiftUI
import NeuroNavKit

// Registration is handled by Sign in with Apple - this view is no longer used
struct RegisterView: View {
    var body: some View {
        Text("Usa Sign in with Apple para registrarte")
    }
}
